{{- if .Values.tls.useCertManager }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "ndots-webhook.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ndots-webhook.labels" . | nindent 4 }}
spec:
  secretName: {{ include "ndots-webhook.tlsSecretName" . }}
  duration: {{ .Values.tls.certManager.duration }}
  renewBefore: {{ .Values.tls.certManager.renewBefore }}
  subject:
    organizations:
      - ndots-webhook
  isCA: false
  privateKey:
    algorithm: RSA
    encoding: PKCS1
    size: 2048
  usages:
    - server auth
    - digital signature
    - key encipherment
  dnsNames:
    - {{ include "ndots-webhook.fullname" . }}
    - {{ include "ndots-webhook.fullname" . }}.{{ .Release.Namespace }}
    - {{ include "ndots-webhook.fullname" . }}.{{ .Release.Namespace }}.svc
    - {{ include "ndots-webhook.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
    name: {{ include "ndots-webhook.issuerName" . }}
    kind: {{ .Values.tls.certManager.issuer.kind }}
{{- end }}
